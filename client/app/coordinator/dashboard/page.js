'use client';import {useEffect,useState} from 'react';import api from '../../../services/api';import PageHeader from '../../../components/PageHeader';import StatCard from '../../../components/StatCard';import Loading from '../../../components/Loading';import {GraduationCap,Files,Megaphone,CircleHelp,Layers3} from 'lucide-react';
export default function Dashboard(){const [d,setD]=useState(null);useEffect(()=>{api.get('/dashboard/coordinator').then(r=>setD(r.data.data)).catch(()=>setD({}))},[]);if(!d)return <Loading/>;const s=d.stats||{};return <div className="page"><PageHeader title="Coordinator Dashboard" description="Manage students, training resources and communication."/><div className="grid-4"><StatCard label="Students" value={s.totalStudents} icon={GraduationCap}/><StatCard label="Active Batches" value={s.activeBatches} icon={Layers3}/><StatCard label="Published Documents" value={s.documents} icon={Files}/><StatCard label="Announcements" value={s.announcements} icon={Megaphone}/><StatCard label="Open Questions" value={s.openQuestions} icon={CircleHelp}/></div><div className="card" style={{marginTop:18}}><h3>Recent Questions</h3><div className="list">{(d.recentQuestions||[]).map(q=><div className="list-item" key={q._id}><strong>{q.subject}</strong><div className="small muted">{q.studentId?.userId?.name||'Student'} • {q.status}</div></div>)}</div></div></div>}
