'use client';import {useEffect,useState} from 'react';import api from '../../../services/api';import PageHeader from '../../../components/PageHeader';export default function FAQ(){const [rows,setRows]=useState([]),[q,setQ]=useState('');useEffect(()=>{api.get('/questions/faq',{params:{search:q}}).then(r=>setRows(r.data.data)).catch(()=>setRows([]))},[q]);return <div className="page"><PageHeader title="Frequently Asked Questions" description="Search resolved questions shared by the training committee."/><div className="toolbar"><input className="input" value={q} onChange={e=>setQ(e.target.value)} placeholder="Search FAQ..."/></div><div className="list">{rows.map(x=><div className="list-item" key={x._id}><strong>{x.subject}</strong><p className="muted">{x.message}</p>{x.latestAnswer&&<div className="notice">{x.latestAnswer}</div>}</div>)}</div></div>}
