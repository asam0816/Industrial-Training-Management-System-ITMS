'use client';import PageHeader from '../../../components/PageHeader';export default function Settings(){return <div className="page"><PageHeader title="System Settings"/><div className="card"><h3>ITMS Configuration</h3><p className="muted">Core configuration is managed using environment variables. User-facing settings can be extended here without changing the project architecture.</p><div className="notice">For password settings use the Security page. File size, database URI, client URL and token secrets are configured in the server .env file.</div></div></div>}
