'use client';import {useEffect,useState} from 'react';import api from '../../../services/api';import PageHeader from '../../../components/PageHeader';import StatCard from '../../../components/StatCard';import Loading from '../../../components/Loading';import {Users,Star} from 'lucide-react';import {ResponsiveContainer,BarChart,Bar,XAxis,YAxis,Tooltip,CartesianGrid} from 'recharts';
export default function EvaluationStats(){const [d,setD]=useState(null);useEffect(()=>{api.get('/evaluations/stats').then(r=>setD(r.data.data)).catch(()=>setD({}))},[]);if(!d)return <Loading/>;const avg=d.averages||{};const chart=Object.entries(avg).map(([name,value])=>({name,value:Number(value||0)}));const exportCsv=async()=>{const r=await api.get('/evaluations/export',{responseType:'blob'});const u=URL.createObjectURL(r.data);const a=document.createElement('a');a.href=u;a.download='evaluation-results.csv';a.click();URL.revokeObjectURL(u)};return <div className="page"><PageHeader title="Evaluation Analytics" description="Usability results for the research evaluation." action={<button className="btn btn-secondary" onClick={exportCsv}>Export CSV</button>}/><div className="grid-4"><StatCard label="Respondents" value={d.respondents||0} icon={Users}/><StatCard label="Usefulness" value={avg.usefulness?.toFixed?.(2)||avg.usefulness||0} icon={Star}/><StatCard label="Ease of Use" value={avg.easeOfUse?.toFixed?.(2)||avg.easeOfUse||0} icon={Star}/><StatCard label="Satisfaction" value={avg.overallSatisfaction?.toFixed?.(2)||avg.overallSatisfaction||0} icon={Star}/></div><div className="card" style={{marginTop:18}}><h3>Average Score by Question</h3><div className="chart-box"><ResponsiveContainer width="100%" height="100%"><BarChart data={chart}><CartesianGrid strokeDasharray="3 3"/><XAxis dataKey="name" hide/><YAxis domain={[0,5]}/><Tooltip/><Bar dataKey="value" fill="#2563eb" radius={[6,6,0,0]}/></BarChart></ResponsiveContainer></div></div></div>}
