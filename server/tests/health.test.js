import request from 'supertest';import app from '../src/app.js';describe('ITMS API',()=>{test('GET /api/health returns healthy status',async()=>{const res=await request(app).get('/api/health');expect(res.status).toBe(200);expect(res.body.success).toBe(true)})});
