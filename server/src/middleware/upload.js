import multer from 'multer';import path from 'path';import fs from 'fs';import {v4 as uuid} from 'uuid';import {env} from '../config/env.js';import {ApiError} from '../utils/ApiError.js';
const dir=path.resolve(process.cwd(),env.uploadDir);fs.mkdirSync(dir,{recursive:true});const allowed=new Map([['application/pdf','.pdf'],['application/msword','.doc'],['application/vnd.openxmlformats-officedocument.wordprocessingml.document','.docx'],['application/vnd.ms-excel','.xls'],['application/vnd.openxmlformats-officedocument.spreadsheetml.sheet','.xlsx']]);const storage=multer.diskStorage({destination:(_r,_f,cb)=>cb(null,dir),filename:(_r,f,cb)=>cb(null,uuid()+ (allowed.get(f.mimetype)||path.extname(f.originalname).toLowerCase()))});export const upload=multer({storage,limits:{fileSize:env.maxFileSizeMb*1024*1024},fileFilter:(_r,f,cb)=>allowed.has(f.mimetype)?cb(null,true):cb(new ApiError(400,'Invalid file type','INVALID_FILE_TYPE'))});
