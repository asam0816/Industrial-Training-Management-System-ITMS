import multer from 'multer';export function notFound(req,res){res.status(404).json({success:false,message:'API endpoint not found',code:'NOT_FOUND'})}export function errorHandler(err,req,res,next){let status=err.status||500,code=err.code||'INTERNAL_SERVER_ERROR',message=err.message||'Internal server error';if(err.name==='ValidationError'){status=400;code='VALIDATION_ERROR'}if(err.name==='CastError'){status=400;code='VALIDATION_ERROR';message='Invalid identifier'}if(err?.code===11000){status=409;code='DUPLICATE_VALUE';message=`Duplicate value for ${Object.keys(err.keyPattern||{})[0]||'field'}`}if(err instanceof multer.MulterError){status=400;code=err.code==='LIMIT_FILE_SIZE'?'FILE_TOO_LARGE':'UPLOAD_ERROR';message=err.message}if(process.env.NODE_ENV!=='production')console.error(err);res.status(status).json({success:false,message:status===500&&process.env.NODE_ENV==='production'?'Internal server error':message,code})}
