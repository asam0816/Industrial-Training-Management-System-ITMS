import mongoose from 'mongoose';const schema=new mongoose.Schema({batchName:{type:String,required:true,trim:true},batchCode:{type:String,required:true,unique:true,trim:true},programme:{type:String,required:true},academicYear:String,startDate:Date,endDate:Date,description:String,status:{type:String,enum:['ACTIVE','COMPLETED','ARCHIVED'],default:'ACTIVE',index:true},createdBy:{type:mongoose.Schema.Types.ObjectId,ref:'User'}},{timestamps:true});export default mongoose.model('Batch',schema);
