import {Router} from 'express';import {authenticate} from '../middleware/auth.js';import {authorize} from '../middleware/authorize.js';import {listUsers,getUser,createUser,updateUser,updateStatus,adminResetPassword} from '../controllers/userController.js';const r=Router();r.use(authenticate,authorize('ADMIN'));r.get('/',listUsers);r.post('/',createUser);r.get('/:id',getUser);r.patch('/:id',updateUser);r.patch('/:id/status',updateStatus);r.post('/:id/reset-password',adminResetPassword);export default r;
