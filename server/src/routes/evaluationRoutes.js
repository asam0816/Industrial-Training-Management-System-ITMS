import {Router} from 'express';import {authenticate} from '../middleware/auth.js';import {authorize} from '../middleware/authorize.js';import {submitEvaluation,myEvaluation,evaluationStats,exportEvaluations} from '../controllers/evaluationController.js';const r=Router();r.use(authenticate);r.post('/',authorize('STUDENT','COORDINATOR'),submitEvaluation);r.get('/me',authorize('STUDENT','COORDINATOR'),myEvaluation);r.get('/stats',authorize('ADMIN'),evaluationStats);r.get('/export',authorize('ADMIN'),exportEvaluations);export default r;
