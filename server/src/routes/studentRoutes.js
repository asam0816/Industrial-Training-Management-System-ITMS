import {Router} from 'express';import {authenticate} from '../middleware/auth.js';import {authorize} from '../middleware/authorize.js';import {listStudents,getStudent,createStudent,updateStudent,deleteStudent} from '../controllers/studentController.js';const r=Router();r.use(authenticate);r.get('/',authorize('ADMIN','COORDINATOR'),listStudents);r.post('/',authorize('ADMIN'),createStudent);r.get('/:id',authorize('ADMIN','COORDINATOR'),getStudent);r.patch('/:id',authorize('ADMIN'),updateStudent);r.delete('/:id',authorize('ADMIN'),deleteStudent);export default r;
