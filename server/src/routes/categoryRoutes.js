import {Router} from 'express';import {authenticate} from '../middleware/auth.js';import {authorize} from '../middleware/authorize.js';import {listCategories,createCategory,updateCategory,deleteCategory} from '../controllers/categoryController.js';const r=Router();r.use(authenticate);r.get('/',listCategories);r.post('/',authorize('ADMIN','COORDINATOR'),createCategory);r.patch('/:id',authorize('ADMIN','COORDINATOR'),updateCategory);r.delete('/:id',authorize('ADMIN'),deleteCategory);export default r;
