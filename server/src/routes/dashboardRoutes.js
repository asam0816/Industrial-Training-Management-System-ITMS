import {Router} from 'express';import {authenticate} from '../middleware/auth.js';import {authorize} from '../middleware/authorize.js';import {adminDashboard,coordinatorDashboard,studentDashboard} from '../controllers/dashboardController.js';const r=Router();r.use(authenticate);r.get('/admin',authorize('ADMIN'),adminDashboard);r.get('/coordinator',authorize('COORDINATOR','ADMIN'),coordinatorDashboard);r.get('/student',authorize('STUDENT'),studentDashboard);export default r;
