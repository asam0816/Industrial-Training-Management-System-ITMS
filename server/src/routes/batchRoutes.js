import {Router} from 'express';import {authenticate} from '../middleware/auth.js';import {authorize} from '../middleware/authorize.js';import {listBatches,getBatch,createBatch,updateBatch,deleteBatch,batchStudents} from '../controllers/batchController.js';const r=Router();r.use(authenticate);r.get('/',listBatches);r.get('/:id',getBatch);r.get('/:id/students',authorize('ADMIN','COORDINATOR'),batchStudents);r.post('/',authorize('ADMIN'),createBatch);r.patch('/:id',authorize('ADMIN'),updateBatch);r.delete('/:id',authorize('ADMIN'),deleteBatch);export default r;
