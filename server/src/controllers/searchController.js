import Document from '../models/Document.js';import Announcement from '../models/Announcement.js';import Question from '../models/Question.js';import Student from '../models/Student.js';import User from '../models/User.js';import {asyncHandler} from '../utils/asyncHandler.js';export const globalSearch=asyncHandler(async(req,res)=>{const q=String(req.query.q||'').trim();if(!q)return res.json({success:true,data:{documents:[],announcements:[],faqs:[],students:[],users:[]}});const rx={$regex:q,$options:'i'};let student=null;if(req.user.role==='STUDENT')student=await Student.findOne({userId:req.user._id});const target=req.user.role==='STUDENT'?{$or:[{targetType:'ALL'},{targetType:'BATCH',targetBatches:student?.batchId}]}:{};const documentQuery={isPublished:true,$and:[target,{$or:[{title:rx},{description:rx},{tags:rx}]}]};const announcementQuery={isPublished:true,$and:[target,{$or:[{title:rx},{content:rx}]}]};const [documents,announcements,faqs]=await Promise.all([Document.find(documentQuery).limit(10),Announcement.find(announcementQuery).limit(10),Question.find({isPublicFAQ:true,$or:[{subject:rx},{message:rx}]}).limit(10)]);let students=[],users=[];if(req.user.role!=='STUDENT'){students=await Student.find({$or:[{studentNumber:rx},{programme:rx}]}).populate('userId','name email').limit(10);if(req.user.role==='ADMIN')users=await User.find({$or:[{name:rx},{email:rx},{username:rx}]}).limit(10)}res.json({success:true,data:{documents,announcements,faqs,students,users}})});
